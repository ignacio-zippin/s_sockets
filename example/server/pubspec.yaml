name: socket_example_2_server
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.0.5

# Add regular dependencies here.
dependencies:
  socket_io: ^1.0.1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
